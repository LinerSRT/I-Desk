<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageButton
        android:id="@+id/expandAppBar"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="end"
        android:layout_marginEnd="@dimen/contentMargin"
        android:layout_marginBottom="@dimen/contentMargin"
        android:background="@drawable/circle_bg"
        android:padding="@dimen/contentMarginHalf"
        android:rotation="0"
        android:src="@drawable/arrow_up" />

    <com.liner.views.ExpandLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/round_corner_background_top"
        app:el_expanded="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/round_corner_background_top"
            android:orientation="horizontal"
            android:padding="@dimen/contentMargin">

            <com.liner.views.YSTextView
                android:id="@+id/requestsItem"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center"
                android:paddingStart="@dimen/contentMargin"
                android:paddingEnd="@dimen/contentMargin"
                android:background="@drawable/round_corner_background"
                android:drawableStart="@drawable/requests_icon"
                android:text="Заявки"
                android:textColor="#383838"
                android:textSize="@dimen/secondaryTextSize"
                app:ys_type="Light" />


            <View
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1" />

            <com.liner.views.YSTextView
                android:id="@+id/profileItem"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center"
                android:paddingStart="@dimen/contentMargin"
                android:background="@drawable/round_corner_background"
                android:paddingEnd="@dimen/contentMargin"
                android:drawableStart="@drawable/user_icon"
                android:text="Профиль"
                android:textColor="#707070"
                android:textSize="@dimen/secondaryTextSize"
                app:ys_type="Light" />

        </LinearLayout>


    </com.liner.views.ExpandLayout>
</LinearLayout>